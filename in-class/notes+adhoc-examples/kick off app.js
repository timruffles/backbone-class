<div> <!-- app view -->
  <nav></nav>
  <!-- 
  child = new Child; 
  child.render(); 
  this.el.html(""); 
  this.currentView.remove()
  this.currentView = child
  this.el.appendChild(child.el);
  
  nodes // 10,000
  batch(node,1000)
  -->
  <div id=main-stage>
      <!-- whichever view needs to be displayed ->
  </div>
</div>